#include <bits/stdc++.h>

using namespace std;

void solve()
{
    // good luck big guy

    // wow this is the first document that I've had the chance of writing since hearing the IOI news. There is this newfound weight to my fingers, I'm not exactly sure what this means for the future but I like it.

    // i bid you well as I travel to get busy with homework during this load shedding session.

    
}

int main()
{
    auto start = chrono::high_resolution_clock::now();
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t = 1;
    // cin >> t;
    for(int i = 0; i < t; i++){
        // cout << "Case #" << i + 1 << ": ";
        solve();
    }
    auto stop = chrono::high_resolution_clock::now(); 
    auto duration = chrono::duration_cast<chrono::milliseconds>(stop - start); 
    // cout <<"Time taken : "<<duration.count() <<" milliseconds\n";
}

