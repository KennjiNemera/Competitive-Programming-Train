#include <bits/stdc++.h>

using namespace std;

// the defines are pretty standard...I dont think that I'll have to memorize that

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
}
