import java.io.*;
import java.util.*;

public class SonyaAndQueries {

  static TreeMap<Long, Integer> multiset;
  static HashMap<String, List<Long>> patternmap;
  static HashMap<Long, String> pattern;

  static void add(Long a) {
    multiset.putIfAbsent(a, 0);
    multiset.put(a, multiset.get(a));
  }

  static void remove(Long a) {
    multiset.put(a, multiset.get(a) - 1);
    if (multiset.get(a) == 0) {
      multiset.remove(a);
    }
  }
  public static void main(String[] args) throws IOException{
    FastReader fr = new FastReader();
    PrintWriter pr = new PrintWriter(new OutputStreamWriter(System.out));
    int n = fr.nextInt();

    multiset = new TreeMap<>();
    patternmap = new HashMap<>();
    pattern = new HashMap<>();


  }

  static class Pair {
      int x, y;
      public Pair(int x, int y) {
          this.x = x;
          this.y = y;
      }
  }

  static int toInt(String s) {
    return Integer.parseInt(s);
  }

  static class FastReader 
  { 
      BufferedReader br; 
      StringTokenizer st; 

      public FastReader() throws FileNotFoundException 
      { 
          br = new BufferedReader(new InputStreamReader(System.in)); 
      } 

      String next() 
      { 
          while (st == null || !st.hasMoreElements()) 
          { 
              try
              { 
                  st = new StringTokenizer(br.readLine()); 
              } 
              catch (IOException  e) 
              { 
                  e.printStackTrace(); 
              } 
          } 
          return st.nextToken(); 
      } 

      int nextInt() 
      { 
          return Integer.parseInt(next()); 
      } 

      long nextLong() 
      { 
          return Long.parseLong(next()); 
      } 

      double nextDouble() 
      { 
          return Double.parseDouble(next()); 
      } 

      String nextLine() 
      { 
          String str = ""; 
          try
          { 
              str = br.readLine(); 
          } 
          catch (IOException e) 
          { 
              e.printStackTrace(); 
          } 
          return str; 
      } 
  }
}
